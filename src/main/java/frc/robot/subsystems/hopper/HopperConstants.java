package frc.robot.subsystems.hopper;

/**
 * Constants unique to the hopper and only needed inside the hopper package.
 */
public class HopperConstants {
    /** Percent power while intaking. */
    final static double INTAKE_DUTY_CYCLE = 0.5;
    /** Percent power while trying to clear a jam. */
    final static double AGITATION_DUTY_CYCLE = -0.5;
}
